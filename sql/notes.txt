control shift pip
mysql runquery (to pop up right side)


CREATE TABLE tablename (username TEXT NOT NULL PRIMARY KEY, pw NOT NULL, email NOT NULL);

- comment out, so u know its therr


control shift p
mysql open database